git status
git init
git add .
git add 檔名
git add *.png
git commit -m "更改內容，版本名稱"
git log
git log --oneline
git push
git clone 網址
git reset --hard log上ID // 不可逆
git checkout log上ID --檔名 // 執行後要進行提交
git diff log上ID --檔名 // 兩記錄點的檔案進行比較